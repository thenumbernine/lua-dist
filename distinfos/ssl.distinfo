-- luassl
name = 'ssl'

local soext = targetOS == 'Windows' and 'dll' or 'so'

files = {
	'ssl.lua',
	'ssl/https.lua',

	-- these are folders that contain core.so-ext
	-- how should I organize this?
	'bin/'..targetOS..'/'..targetArch..'/ssl.'..soext,
}
deps = {
	'socket',
}
